name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 9 * * 1,4'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" -X GET "https://nljgejiggwybjfdssjsy.supabase.co/rest/v1/inventory?select=id&limit=1" -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5samdlamlnZ3d5YmpmZHNzanN5Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM3ODIyODYsImV4cCI6MjA4OTM1ODI4Nn0.L_lF9eGgjh-7BW0-LaF_XmJdNcf4mtFxPYVlugbG3KQ")
          echo "HTTP status: $STATUS"
          if [ "$STATUS" -ne "200" ]; then exit 1; fi
          echo "Supabase ping OK"